import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mapView: {
    width: '100%',
    height: '100%',
  },
  marker: {
    width: 50,
    height: 50,
    resizeMode: 'contain',
  },
});

export default styles;
