import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  confirmButton: {
    width: '100%',
    backgroundColor: '#000',
    padding: 10,
    margin: 10,
    alignItems: 'center',
  },
  confirmText: {
    color: '#fff',
    fontWeight: 'bold',
  },
});

export default styles;
